# frozen_string_literal: true

RSpec.describe My::Clickup do
  it "has a version number" do
    expect(My::Clickup::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
